{/*import React from 'react';

const SearchButton: React.FC = () => {
  return (
    <button
      type="submit"
      className="bg-indigo-500 text-white px-6 py-3 rounded-r-lg hover:bg-indigo-400 transition-colors"
    >
      Search
    </button>
  );
};

export default SearchButton;
*/}

import React from 'react';

const SearchButton: React.FC = () => {
  return (
    <button
      type="submit"
      className="bg-blue-500 text-white px-6 py-3 rounded-r-lg hover:bg-blue-400 transition-colors"
    >
      Search
    </button>
  );
};

export default SearchButton;
